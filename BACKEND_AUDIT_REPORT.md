# 后端业务逻辑与测试审计报告

审计日期：2026-08-18

## 结论

当前实现已经达到“单实例可部署、可联调、关键交易路径有保护”的 MVP 水平，但不能直接认定为多实例、高资金安全等级的最终生产版本。关键服务层、权限拦截器和加密逻辑现有 30 条单元测试全部通过，并完成真实 MySQL/Redis HTTP 回归。

## 已检查与修复

| 领域 | 检查结果 |
| --- | --- |
| 卡密核销 | 行锁 + CAS 防重复核销；同一事务更新用户、卡密、财务和审计；拒绝客户端篡改金额、停用套餐和跨设备覆盖 |
| 试用 | 限制单手机号一次；增加验证码环境配置校验；跨设备不能覆盖绑定 |
| 客户端认证 | 会话、手机号、MySQL 设备绑定和 Redis 活跃绑定四项一致性检查；注销与解绑语义分离 |
| 管理权限 | 五类角色采用后端权限矩阵强制校验；代理商卡密查询按当前管理员隔离 |
| 小号资源 | 领取后立即标记 `BUSY`；未上报租约定时回收；每日计数定时重置；完整 Token 不再返回管理端 |
| 使用结果 | Redis Lua 原子消费租约；`req_uuid` 唯一流水提供重复上报幂等；成功扣 1，网络/业务/封号失败扣 0；封号资源拉黑 |
| 财务 | 收入和卡密物理分表；采购数量、单价、供应商校验；采购人取当前登录管理员而不是前端参数 |
| 审计 | 制卡、资源录入/状态变更、管理员解绑、采购入账、客户端激活写入不可逆审计表 |
| 数据库初始化 | JDBC 自动建库 + Spring SQL Init；种子幂等；SQL 失败阻止应用启动 |
| 部署可观测性 | 增加 `/actuator/health`；MySQL 或 Redis 异常会反映在健康状态 |

## 单元测试矩阵

| 测试类 | 数量 | 覆盖重点 |
| --- | ---: | --- |
| `AdminAuthInterceptorTest` | 3 | 管理员角色授权、越权、禁用账号 |
| `DeviceSecurityInterceptorTest` | 2 | 手机号与设备匹配、异机拦截 |
| `RolePermissionsTest` | 4 | 超管、代理、客服和未知角色权限 |
| `AdminAuditServiceTest` | 1 | 操作人、角色、目标和代理 IP 留痕 |
| `CardKeyActivationServiceImplTest` | 6 | 正常核销、重复卡、金额篡改、异机、停用套餐、试用限制 |
| `DispatchGatewayServiceImplTest` | 7 | 领取、过期/配额/时钟、成功扣次、失败免责、封号、幂等、租约过期 |
| `ResourceLeaseServiceTest` | 2 | 租约 TTL 和 Lua 结果解析 |
| `DeviceBindingServiceTest` | 2 | Redis 绑定和故障降级 |
| `FinancialServiceImplTest` | 2 | 收支汇总、利润率、采购边界 |
| `AesByteFlipUtilsTest` | 1 | AES-GCM 加解密一致性 |
| 合计 | 30 | 全部通过 |

## 真实环境回归结果

- Spring Boot 在现有 MySQL/Redis 环境启动成功，健康检查为 `UP`。
- SQL 初始化连续多次启动无重复种子错误。
- 资源领取后第二次并发领取返回 `50301`。
- 同一租约首次上报扣 1，重复上报扣 0，实际次数差值为 1。
- 失败上报不扣次数并释放资源，后续可再次领取。
- 管理资源列表返回脱敏值，例如 `demo****e_me`。
- 代理角色访问财务接口返回 `40310`。
- 客户端解绑后可在新设备登录，旧设备不再满足绑定校验。

## 尚未完全覆盖的生产级项目

1. 缺少 Testcontainers 驱动的 MySQL/Redis 自动集成测试；当前真实回归是人工 HTTP 脚本执行。
2. 缺少高并发压力测试，尚未量化数百客户端同时领取、上报时的吞吐和锁等待。
3. Sa-Token 会话仍在单进程内存中，不能直接部署多个后端副本。
4. Redis Lua 与 MySQL 事务之间不是分布式事务；数据库提交失败时依赖 BUSY 超时回收。
5. 短信验证码仍是环境变量演示实现，缺少真实短信、发送频控和验证码一次性消费。
6. 管理员密码仍为 pepper + SHA-256，缺少 BCrypt/Argon2、失败锁定和 MFA。
7. SQL 初始化没有版本号，已有数据库字段变更必须人工执行迁移；建议引入 Flyway。
8. 缺少前端 E2E、接口契约快照、备份恢复演练和故障注入测试。

因此，当前版本适合内测、单机交付和业务验证。对公网正式商业运行前，应至少完成上述第 1、3、5、6、7 项。
